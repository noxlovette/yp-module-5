[package]
name = "broken-app"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports", "csv_output"] }

[[bench]]
name = "baseline"
harness = false

[[bench]]
name = "criterion"
harness = false

[profile.release]
debug = true
